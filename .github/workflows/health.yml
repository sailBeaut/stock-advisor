name: Wake Render

on:
  schedule:
    - cron: '*/14 13-22 * * 1-5'   # Mon-Fri 13:00-22:00 UTC
  workflow_dispatch:                 # ← ADD THIS LINE

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://stock-advisor-api-n5xr.onrender.com/health || echo "API unreachable"